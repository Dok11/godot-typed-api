import type { CurveXYZTexture, GodotArray, GodotDictionary, VisualShaderNodeResizableBase, float, int } from "../index.d.ts";
/**
 * Performs a `CurveXYZTexture` lookup within the visual shader graph.
 * 
 * Comes with a built-in editor for texture's curves.
 */
export class VisualShaderNodeCurveXYZTexture extends VisualShaderNodeResizableBase {
/**
 * The source texture.
 */
  texture: CurveXYZTexture;
}
